PROXY_ENDPOINT=rpc://regtest.thunderstack.org:3000/json-rpc
INDEXER_URL=regtest.thunderstack.org:50001
NETWORK=3

# Remote Redis with authentication (Redis Cloud):

# Refresh worker configuration
REFRESH_INTERVAL=100
MAX_REFRESH_RETRIES=10
RETRY_DELAY_BASE=5
POLL_INTERVAL=1
WATCHER_TTL=86400
# PostgreSQL configuration (for refresh queue and watchers)
POSTGRES_URL=postgresql://postgres:postgres@localhost:5432/rgb_node
ENABLE_RECOVERY=true
